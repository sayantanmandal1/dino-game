# Copy to .env and edit as needed.
ENVIRONMENT=production
FRONTEND_PORT=3000
BACKEND_PORT=8000
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]
# NEAT_WORKERS: "auto" uses (cpu_count - 1); set to "1" to force serial eval (required on Windows native)
NEAT_WORKERS=auto
